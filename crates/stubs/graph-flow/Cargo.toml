[package]
name = "graph-flow"
version = "0.1.0"
edition = "2021"
description = "Stub for rs-graph-llm graph-flow — replaced by real crate when available"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
