[build]
builder = "dockerfile"
# Dockerfile lives at repo root. Live-clones all deps from GitHub — no
# sibling directories needed. Build context = this repo (q2/).
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 10
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
