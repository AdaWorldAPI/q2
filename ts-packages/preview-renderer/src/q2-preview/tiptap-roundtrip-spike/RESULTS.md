# tiptap round-trip spike — results

<!-- GENERATED by roundtrip.test.ts (bd-sjb4pzx8). Throwaway spike evidence. -->

Verdicts: exact=15, equivalent=1, broken=0

## plain-para — `exact`

```` qmd in ````
A simple paragraph of prose with nothing special in it.
```` md out (PM → markdown) ````
A simple paragraph of prose with nothing special in it.

## para-inline-formatting — `exact`

```` qmd in ````
A paragraph with **bold**, *italic*, `inline code`, and a [link](https://example.com).
```` md out (PM → markdown) ````
A paragraph with **bold**, *italic*, `inline code`, and a [link](https://example.com).

## atx-heading — `exact`

```` qmd in ````
## A second-level heading
```` md out (PM → markdown) ````
## A second-level heading

## bullet-list — `exact`

```` qmd in ````
- first item
- second item
- third item
```` md out (PM → markdown) ````
* first item
* second item
* third item

## ordered-list — `exact`

```` qmd in ````
1. alpha
2. beta
3. gamma
```` md out (PM → markdown) ````
1. alpha
2. beta
3. gamma

## nested-list — `exact`

```` qmd in ````
- top
    - nested a
    - nested b
- back to top
```` md out (PM → markdown) ````
* top
  * nested a
  * nested b
* back to top

## blockquote — `equivalent`

```` qmd in ````
> a quoted line
> continued on the next line
```` md out (PM → markdown) ````
> a quoted line continued on the next line

## code-block-plain — `exact`

```` qmd in ````
```
plain code
no language
```
```` md out (PM → markdown) ````
```
plain code
no language
```

## code-block-quarto — `exact`

```` qmd in ````
```{python}
print("hi")
```
```` md out (PM → markdown) ````
```{python}
print("hi")
```

## shortcode — `exact` (1 chip)

```` qmd in ````
Watch this: {{< video https://youtu.be/abc >}} for details.
```` md out (PM → markdown) ````
Watch this: {{< video https://youtu.be/abc >}} for details.

## inline-math — `exact` (1 chip)

```` qmd in ````
The famous identity $e^{i\pi} + 1 = 0$ is elegant.
```` md out (PM → markdown) ````
The famous identity $e^{i\pi} + 1 = 0$ is elegant.

## display-math — `exact` (1 chip)

```` qmd in ````
$$\int_0^1 x^2 \, dx = \frac{1}{3}$$
```` md out (PM → markdown) ````
$$\int_0^1 x^2 \, dx = \frac{1}{3}$$

## crossref — `exact` (1 chip)

```` qmd in ````
As shown in @fig-plot, the trend is clear.
```` md out (PM → markdown) ````
As shown in @fig-plot, the trend is clear.

## citation — `exact` (1 chip)

```` qmd in ````
This is well established [@knuth1984].
```` md out (PM → markdown) ````
This is well established [@knuth1984].

## callout-div — `exact` (1 chip)

```` qmd in ````
::: {.callout-note}
This is a note.
:::
```` md out (PM → markdown) ````
::: {.callout-note}
This is a note.
:::

## raw-html-inline — `exact` (2 chips)

```` qmd in ````
Some text with <span class="x">raw html</span> inside.
```` md out (PM → markdown) ````
Some text with <span class="x">raw html</span> inside.
