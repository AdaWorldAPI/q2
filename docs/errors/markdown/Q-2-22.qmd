---
title: "Unclosed Editorial Highlight"
description: "An editorial-highlight span opens with `[>>` but no closing `]` appears before the end of the block."
code: Q-2-22
subsystem: markdown
status: stub
since: "99.9.9"
categories:
  - markdown
---

# `Q-2-22` — Unclosed Editorial Highlight

> An editorial-highlight span opens with `[>>` but no closing `]`
> appears before the end of the block.

## What this means

The editorial-highlight form `[>>marked text]` flags text for
visual emphasis without otherwise changing it. This error fires
when the opener `[>>` has no matching close.

## Why this happens

A deleted closing bracket, or a literal `[>>` taken as an
editorial opener.

## How to fix

Add the closing `]`, or escape the opening bracket: `\[>>text]`.

## Example

```markdown
[>>
```

## Related

- [`Q-2-19`](Q-2-19.qmd) — unclosed editorial insert (`[++`).
- [`Q-2-20`](Q-2-20.qmd) — unclosed editorial delete (`[--`).
- [`Q-2-21`](Q-2-21.qmd) — unclosed editorial comment (`[!!`).
